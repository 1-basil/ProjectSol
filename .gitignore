node_modules/
target/
dist/
.anchor/
test-ledger/
*.log
.env
.env.*
!.env.example
*.db
*.db-journal
*.db-wal
*.db-shm

# Disposable devnet-only test keypairs generated by the controlled E2E
# harness -- never a production key, never committed regardless.
apps/prototype-server/scripts/devnet-e2e/keys/
apps/prototype-server/scripts/devnet-e2e/*.db
apps/prototype-server/scripts/devnet-e2e/*.json

# Ephemeral devnet-test allowlist fixture (see prepDevnetAllowlistFixture.ts)
# -- NEVER the real allowlist, never committed regardless.
apps/prototype-server/data/allowlist/devnet-test-fixture-EPHEMERAL.json

# Wallet keypairs / key-value pool state -- never commit
apps/prototype-server/pooled-wallet*.json
apps/prototype-server/pool_k_v.json
*keypair*.json

# Build output / caches
.next/
*.tsbuildinfo
.claude/

# Design asset source files
apps/web/popular-cryptocurrency-logos-set/
