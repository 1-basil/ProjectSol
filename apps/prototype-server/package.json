{
  "name": "@platform/prototype-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Prototype deposit flow: wallet scan, SOL/wSOL + SPL authorization and sweep, per-asset $1M cap, dashboard API.",
  "main": "src/index.ts",
  "scripts": {
    "test": "node --test",
    "start": "node --env-file=.env src/server.ts"
  },
  "dependencies": {
    "@platform/accounting": "*",
    "@solana/spl-token": "^0.4.15",
    "@solana/web3.js": "^1.98.4",
    "bs58": "^4.0.1"
  },
  "engines": {
    "node": ">=22"
  }
}
